{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "publicBooks": {
      "$bookId": {
        ".read": true,
        ".write": true
      }
    }
  }
}
